# frozen_string_literal: true

require 'rubygems'
require 'bundler/setup'
require 'pathname'
require "#{__dir__}/lib/swag_dev-project"
require 'swag_dev/project'

pp SwagDev::Project.new
pp SwagDev::Project.version_info

# Dir.glob('rake/**/*.rake').each {|f| load(f)}
